# --- Agent runtime ---
strands-agents>=0.1.0        # Strands Agents SDK (uses Amazon Bedrock by default)

# --- Web API ---
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0            # production WSGI server (used on Render / Railway / Fly)

# --- Tool dependencies ---
requests>=2.31.0            # HIBP k-anonymity API, RDAP, Google Safe Browsing
zxcvbn>=4.4.28             # password strength estimation

# --- Dev / tests (optional) ---
# pytest>=8.0
